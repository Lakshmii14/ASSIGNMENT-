// Declare two variables with different numerical values
let a = 5;
let b = 10;

// Compare using various comparison operators
console.log("a == b:", a == b);          // Equal to
console.log("a === b:", a === b);        // Strict equal to
console.log("a != b:", a != b);          // Not equal to
console.log("a !== b:", a !== b);        // Strict not equal to
console.log("a > b:", a > b);            // Greater than
console.log("a < b:", a < b);            // Less than
console.log("a >= b:", a >= b);          // Greater than or equal to
console.log("a <= b:", a <= b);          // Less than or equal to


Explanation of Results:
a == b: Checks if a is equal to b (value comparison). 
Result: false
a === b: Checks if a is strictly equal to b (value and type comparison). 
Result: false
a != b: Checks if a is not equal to b (value comparison).
Result: true
a !== b: Checks if a is strictly not equal to b (value and type comparison).
Result: true
a > b: Checks if a is greater than b.
Result: false
a < b: Checks if a is less than b.
Result: true
a >= b: Checks if a is greater than or equal to b. 
Result: false
